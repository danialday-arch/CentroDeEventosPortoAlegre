server:
  port: 8089
  error:
    include-message: always
    include-binding-errors: always

spring:
  application:
    name: ms-orden-cocina
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/portoalegre_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true